<%= form_with(model: country_state, local: true) do |form| %>
  <% if country_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(country_state.errors.count, "error") %> prohibited this country_state from being saved:</h2>

      <ul>
      <% country_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
